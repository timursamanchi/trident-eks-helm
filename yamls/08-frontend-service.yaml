# 06-frontend-service.yaml
apiVersion: v1
kind: Service
metadata: 
  name: quote-frontend
  namespace: quote-app
  labels:
    app: quote-frontend
spec:
  selector:
    app: quote-frontend
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80