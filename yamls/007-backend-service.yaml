# 06-backend-service.yaml
apiVersion: v1
kind: Service
metadata: 
  name: quote-backend
  namespace: quote-app
  labels:
    app: quote-backend
spec:
  selector:
    app: quote-backend
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080