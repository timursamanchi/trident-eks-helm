# 05-backend-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: quote_backend
  namespace: quote_app
  labels:
    app: quote_backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quote_backend
  template:
    metadata:
      labels:
        app: quote_backend
    spec:
      containers:
        - name: backend
          image: timursamanchi/quote_backend:1000
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          imagePullPolicy: Always
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 2
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 10
            timeoutSeconds: 2
            periodSeconds: 15
            failureThreshold: 3