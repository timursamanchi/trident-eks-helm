# 02-busybox.yaml
apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: quote-app
spec:
  containers:
    - name: busybox
      image: busybox
      command: ["sh", "-c", "while true; do echo hello from busybox; sleep 5; done"]
      imagePullPolicy: IfNotPresent
      resources:
        requests:
          cpu: "10m"
          memory: "16Mi"
        limits:
          cpu: "50m"
          memory: "32Mi"
  restartPolicy: Never