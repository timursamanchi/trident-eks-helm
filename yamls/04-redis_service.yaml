# 04-redis_service.yaml
apiVersion: v1
kind: Service
metadata:
  name: quote_redis
  namespace: quote_app
spec:
  type: ClusterIP
  selector:
    app: quote_redis
  ports:
    - port: 6379
      targetPort: 6379
